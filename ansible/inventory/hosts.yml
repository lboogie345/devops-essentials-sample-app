---
# Sample inventory. Real inventories belong in a separate, access-controlled
# repository or a dynamic source; this one exists so `--syntax-check` and
# `ansible-lint` have something to resolve against in CI.
all:
  children:
    rhel8:
      children:
        rhel8_prod:
          hosts:
            rhel8-app-01.example.mil:
            rhel8-app-02.example.mil:
        rhel8_staging:
          hosts:
            rhel8-stage-01.example.mil:
